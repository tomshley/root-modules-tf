# Deployment overlay: staging/gov
#
# Base overlay for gov profile staging environments.
# Gov environments typically have stricter topic inclusion rules.

include:
  - service: service-a
    roles: [core]
  - service: service-b
    roles: [core]

exclude_topics: []
