# Per-environment streaming credentials.
# Copy to staging-us-east-1-streaming.env and fill in the values.
# This file is sourced by the Makefile before init/plan/apply.

CONFLUENT_CLOUD_API_KEY=<confluent-cloud-api-key>
CONFLUENT_CLOUD_API_SECRET=<confluent-cloud-api-secret>

# Kafka admin credentials — consumed by Terraform as TF_VAR_* variables.
# Non-sensitive config (confluent_config, workloads) is in committed terraform.tfvars.
TF_VAR_kafka_admin_api_key=<kafka-admin-api-key>
TF_VAR_kafka_admin_api_secret=<kafka-admin-api-secret>
